{
  "name": "hellextension-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Hell Extension state sync nightmare",
  "type": "module",
  "scripts": {
    "server:pages": "node testpages/server.js",
    "server:api": "cd backend && node api.js",
    "server": "npm run server:api & npm run server:pages",
    "setup:backend": "cd backend && npm install",
    "setup": "npm install && npm run setup:backend && npm run icons",
    "test:sync": "tsx tests/sync_sidebar_to_tab.ts",
    "test:race": "tsx tests/race_condition_chaos.ts",
    "test:storage": "tsx tests/storage_leak.ts",
    "test:all": "npm run test:sync && npm run test:race && npm run test:storage",
    "icons": "cd extension && chmod +x generate_icons.sh && ./generate_icons.sh"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "tsx": "^4.6.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
